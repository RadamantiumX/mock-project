interface Props {
    id?: string;
    keywords?: string;
}

export const VideoSelected:React.FC<Props> = ({ id, keywords }) => {


  return (
    <section>
        <div className="embed-responsive embed-responsive-16by9">
      <iframe className="embed-responsive-item" title="Hairy hot cute face man homo gay sexfree We brought Mario Costa back by popular demand" width="420" height="315" src={`https://eporner.com/embed/${id}`} allow="accelerometer;autoplay;clipboard-write;encrypted-media;gyroscope;picture-in-picture" allowFullScreen>

      </iframe>
  </div>
  </section>
  )
}
